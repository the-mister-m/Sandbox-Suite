Region context files live here, one per region, named `<region id>.md`.
The compiler reads the matching file and adds it as a context layer.
A missing file means no layer. This folder stays empty this phase.
