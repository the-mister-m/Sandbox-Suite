# SPEC — Phase 3.5 — 17 — Sonnet — Skills rewrite

Written 2026-09-14. Runs after 14. Both codecanvas skills rewritten
for HTML-only: layers, plugins, pages, styles, SVG, threading,
masters. Every doc-mode sentence gone.

## Rules for this agent

- Global rules apply: ~/.claude/CLAUDE.md. Recite them first.
- You own injections/skills/codecanvas-howto.md and
  injections/skills/codecanvas-agent.md. Nothing else.
- Stages below. Stage 1 writes the receipt outline. Cap 100K.
- Receipt: Docs/Reports/RECEIPT-phase3.5-17.md. One line each to
  SESSIONLOG.md and INDEX.md.

## Read

- The scope whole.
- Both skills as they stand.
- Every receipt Docs/Reports/RECEIPT-phase3.5-*.md — the PICKS I MADE
  and CONTRACT FIELDS ADDED sections only.
- canvas.js `frame._canvas` block and MOD.defaults, tools.js
  SECTIONS and the inspector group names. Read only.

## Stage 1 — receipt outline. Check.

## Stage 2 — codecanvas-agent.md

Keep the header lines. Drop the DOC PATHWAY. FILE PATHWAY becomes the
whole skill: the document shape (scope 3.1 as a short listing), the
thirteen patch kinds with shapes, every `frame._canvas` name grouped
as the scope groups them, the canvas options with defaults, the
plugin word, the snippets/insertAt path, the styles path, threading,
masters, the DO NOT TOUCH list (master sections, host nodes). Same
voice, same density as today. Check.

## Stage 3 — codecanvas-howto.md

Teach in this order: the page (pages tab), layers (the outliner,
lock/hide, ▲▼◀▶, right-click, keys), placing things (snippets, draw
tools), the inspector (Transform, Paragraph, Columns, Surface, Border,
Fill/Stroke for shapes, Styles), arrange, guides and snap, threading,
masters, save. STATUS WORDS updated with every new one the receipts
name. WORTH SHOWING rewritten. DO NOT TEACH: pen, raster, 3D. Check.

## Stage 4 — receipt

Diff summary in the receipt: sections removed, sections added. Check.

## Done when

- Neither skill contains "json", "doc mode", "kit", "widget type",
  "Export", "schematic", "instruction".
- Every `frame._canvas` name in the code appears in the agent skill.
- Every bar button and panel group in the code appears in the howto.
